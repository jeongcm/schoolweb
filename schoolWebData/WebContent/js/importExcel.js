var width = "width=350,";
var height = "height=100,";
var resizable = "resizable=no,";
var scrollbars = "scrollbars=no,";
var status = "status=no,";

/* 강의 */

function 강의담당상세Up() {
	var popUrl = "강의담당상세Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

function 강의담당Up() {
	var popUrl = "강의담당Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

/* //강의 */

/* 강의 */

function 강의담당외부상세Up() {
	var popUrl = "강의담당상세외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

function 강의담당외부Up() {
	var popUrl = "강의담당외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

/* //강의 */


/* 강의공개실적 */
function 강의공개실적양식() {
	var popUrl = "강의공개실적양식Down.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

function 강의공개실적Up() {
	var popUrl = "강의공개실적Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //강의공개실적 */

/* 교원현황 */

function 교원현황Up() {
	var popUrl = "교원현황Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //교원현황 */

/* 교육프로그램참여비율 */

function 교육프로그램참여비율Up() {
	var popUrl = "교육프로그램참여비율Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

/* //교육프로그램참여비율 */

/* 동아리 */

function 동아리Up() {
	var popUrl = "동아리Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

/* //동아리 */

/* 발전기금 */

function 발전기금조성액상세Up() {
	var popUrl = "발전기금조성액상세Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

function 발전기금조성액Up() {
	var popUrl = "발전기금조성액Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //발전기금 */

/* 봉사실적 */

function 봉사실적Up() {
	var popUrl = "봉사실적Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //봉사실적 */

/* 설문조사 */

function 설문조사Up() {
	var popUrl = "설문조사Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //설문조사 */

/* 수상실적 */

function 수상실적Up() {
	var popUrl = "수상실적Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //수상실적 */

/* 신입생현황 */
function 신입생현황Up() {

	var popUrl = "신입생현황Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //신입생현황 */

/* 신입생현황외부 */

function 신입생현황외부Up() {

	var popUrl = "신입생현황외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //신입생현황외부 */

/* 연구비 */

function 연구비Up() {
	var popUrl = "연구비Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //연구비 */

/* 연구실적 */

function 연구실적Up() {
	var popUrl = "연구실적Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //연구실적 */

/* 외국인학생비율 */

function 외국인학생비율Up() {
	var popUrl = "외국인학생비율Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //외국인학생비율 */

/* 장학금 */

function 교외장학금Up() {
	var popUrl = "교외장학금Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //장학금 */

/* 재학생현황 */
function 재학생현황Up() {
	var popUrl = "재학생현황Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //재학생현황 */

/* 재학생현황2 */
function 재학생현황2Up() {
	var popUrl = "재학생현황2Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //재학생현황2 */

/* 전임교원 */

function 전임교원확보율Up() {
	var popUrl = "전임교원확보율Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //전임교원 */

/* 전임교원1인당강의담당비율d */
function 강의담당외부Up() {
	var popUrl = "강의담당외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //전임교원1인당강의담당비율d */

/* 전임교원1인당교외연구비d */
function 연구비외부Up() {
	var popUrl = "연구비외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //전임교원1인당교외연구비d */

/* 전임교원1인당연구실적d */

function 연구실적외부Up() {
	var popUrl = "연구실적외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //전임교원1인당연구실적d */

/* 중도탈락률 */

function 중도탈락률Up() {
	var popUrl = "중도탈락률Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //중도탈락률 */

/* 중도탈락률외부 */

function 중도탈락률외부Up() {
	var popUrl = "중도탈락률외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //중도탈락률외부 */

/* 국세db취업자양식 */

function 국세db취업자Up() {
	var popUrl = "국세db취업자Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //국세db취업자양식 */

/* 취업자 */

function 취업자Up() {
	var popUrl = "취업자Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업자 */

/* 취업제외자 */
function 취업제외자Up() {
	var popUrl = "취업제외자Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업제외자 */

/* 취업현황 */
function 취업현황Up() {
	var popUrl = "취업현황Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업현황 */
/* //취업 */

/* 취업외부 */
/* 국세db취업자외부 */
function 국세db취업자외부Up() {
	var popUrl = "국세db취업자외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //국세db취업자외부 */

/* 취업률외부 */
/* //취업률외부 */

/* 취업자외부 */
function 취업자외부Up() {
	var popUrl = "취업자외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업자외부 */

/* 취업제외자외부 */
function 취업제외자외부Up() {
	var popUrl = "취업제외자외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업제외자외부 */

/* 취업현황외부 */
function 취업현황외부Up() {
	var popUrl = "취업현황외부Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //취업현황외부 */
/* //취업외부 */

/* 캡스톤디자인 */

function 캡스톤디자인Up() {
	var popUrl = "캡스톤디자인Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //캡스톤디자인 */

/* 특허등록 */

function 특허등록Up() {
	var popUrl = "특허등록Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //특허등록 */

/* 특허등록상세 */

function 특허등록상세Up() {
	var popUrl = "특허등록상세Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //특허등록상세 */

/* 기술이전 */

function 기술이전Up() {
	var popUrl = "기술이전Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //기술이전 */

/* 학과현황 */
function 학과현황Up() {
	var popUrl = "학과현황Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //학과현황 */

/* 현장실습 */

function 현장실습Up() {
	var popUrl = "현장실습Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}
/* //현장실습 */


/* 수주실적 */

function 전임교원1인당국책사업수주실적Up() {
	var popUrl = "전임교원1인당국책사업수주실적Up.jsp";
	var popOption = width + height + resizable + scrollbars + status;

	window.open(popUrl, "", popOption);
}

/* 수주실적*/

function popupOpen() {
	var popUrl = "excel_upload_Form.jsp"; // 팝업창에 출력될 페이지 URL
	var popOption = width + height + resizable + scrollbars + status;
	// 옵션(optoin)
	window.open(popUrl, "", popOption);
}
function Downpop() {
	var popUrl = "excel_download_form.jsp";
	var popOption = width + height + resizable + scrollbars + status;
	// 옵션(optoin)
	window.open(popUrl, "", popOption);
}

function checkForm() {
	if (checkFileType(upload.file.value) == "") {
		return false;
	} else if (checkFileType(upload.file.value) == false) {

		alert("업로드된 파일이 없거나 잘못된 파일입니다. \n엑셀파일만 업로드 해주세요.");
		window.open("about:blank", "_self").close();
		return false;
	} else {
		return true;
	}
	document.upload.submit();

}
function checkFileType(filePath) {

	var fileLen = filePath.length;
	var fileFormat = filePath.substring(fileLen - 4);
	fileFormat = fileFormat.toLowerCase();

	if (fileFormat == "xlsx" || fileFormat == ".xls") {
		return true;
	} else {
		alert("파일이 없거나 잘못된 파일입니다. \n엑셀파일만 업로드 해주세요.(*.xls), (*.xlsx)");
		window.open("about:blank", "_self").close();
		return false;
	}
}
function closePopup() {
	alert("엑셀파일 내 행과 열이 불일치 혹은 입력값에 빈 칸이 존재합니다.");
	window.open("about:blank", "_self").close();
	return false;
}
